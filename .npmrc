# NPM Configuration for QuickPay Mobile App
# This allows installation despite peer dependency conflicts

# Use legacy peer dependency resolution to handle React 19 compatibility
legacy-peer-deps=true